#this is for the AS92004 assessment

#gets random for random number gernerator
import random
#create functions

#function to get input from user



def get_input(input_message, error_message, data_type):
    while True:
        try:
            user_input = data_type(input(input_message))
        except ValueError:
            print(error_message)
            continue
        else:
            return user_input
            break

#function to confirm input from user
def confirm_input(input_message):
    while user_input != 'y' and user_input != 'yes' and user_input != 'n' and user_input != 'no':
        user_input = input(input_message).lower()
        if user_input == 'y' or user_input == 'yes':
            return True
        elif user_input == 'n' or user_input == 'no':
            return False
        else:
            print("Please enter yes or no")

#function for question
questions_list = ['pythguras', 'sin', 'cos', 'tan']
def question():
    question_type = random.choice(questions_list)

    math_num_1 = random.randint(100)
    math_num_2 = random.randint(100)
    math_num_3 = random.randint(100)

    print('Your question is one on ' + str(question_type))
    if question_type == 'pythguras':
        pythguras_question
    if question_type == 'sin':
        sin_question
    if question_type == 'cos':
        cos_question
    if question_type == 'tan':
        tan_question


def pythguras_question():

def cos_question():

def cos_question():

def tan_question():


#main code

#introduction
print('Hello, welcome to the triganomitry and pythagoras theorem quiz')
print('You will be asked a series of questions and you will need to answer them correctly')

#ask user for their name and welcome them
name = get_input("Enter your name: ", "Please enter your name", str)
print('hello, ' + name)


#ask user how many questions they want to answer and confirm amount the user wants
confirm = False
while confirm == False:
    
        number_of_questions = get_input("Enter the amount of questions you want to answer: ", "Please enter a number", int)
    
        confirm = confirm_input('Do you want to have' + number_of_questions + 'questions? ')
        if confirm == True:
            print('Great, lets start the quiz')
        else:
            continue

